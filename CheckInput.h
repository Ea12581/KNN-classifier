#ifndef CHKIN_H
#define CHKIN_H
#include <string.h>
#include <string>
#include <ctype.h>
#include <regex>

using namespace std;

bool isNumber(char[]);
bool isLegalPort(char[]);
bool isLegalIp(string);

#endif